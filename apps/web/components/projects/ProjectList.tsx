'use client'

// Stub — implemented in Project CRUD task
export function ProjectList({ userId }: { userId: string }) {
  return (
    <div className="text-muted-foreground text-sm">
      Loading projects… (coming in next step)
    </div>
  )
}
